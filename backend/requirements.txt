# Core web/runtime dependencies
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0
httpx>=0.27.0

# Optional integrations (install as needed; the app degrades gracefully):
#   openai>=1.0.0        # OpenAI-compatible providers (OpenAI/DeepSeek/Qwen/Moonshot/Zhipu)
#   anthropic>=0.25.0    # Anthropic (Claude) native protocol
#   ddgs>=3.0.0          # DuckDuckGo web search (no API key required)
#   mcp>=1.0.0           # MCP client for external job-platform servers
